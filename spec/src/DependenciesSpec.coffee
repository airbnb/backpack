describe 'dependencies', ->

  it 'should have $', ->
    expect($).toBeDefined()

  it 'should have Underscore', ->
    expect(_).toBeDefined()

  it 'should have Backbone', ->
    expect(Backbone).toBeDefined()

  it 'should have Backbone', ->
    expect(Backpack).toBeDefined()
